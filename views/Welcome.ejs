<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Welcome</title>
</head>
<body>
    <% /* EJS ko install karne ke liye terminal mein yeh command chalata ha: npm install ejs */ %>


    <h2>
        Welcome <%= empInfo.name %> 
    </h2>
    <ul>
        <% empInfo.courses.forEach((item) => { %>
            <li><%= item %></li>
        <% }); %>
    </ul>
</body>
</html>
